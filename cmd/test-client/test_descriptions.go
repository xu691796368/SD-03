// Package main - test_descriptions.go
// 简易测试模式中每个测试用例的详细描述
// 通过 testID 查找对应的描述文本，在执行前展示给用户
package main

// testDescMap 存储所有测试用例的描述（按 ID 索引）
var testDescMap = map[string]string{
	// ========================================================================
	// 协议编解码测试（P01-P06）
	// ========================================================================
	"P01": `【前置场景】协议帧是客户端与服务器通信的基础，必须保证编码→解码后数据完全一致。
【测试过程】构造5种不同类型的请求帧（GET带key、SET带key-value、DELETE带key、INFO无参数、SET空value），
逐一调用 EncodeRequest 编码，再调用 DecodeRequest 解码，对比命令码、Key、Value与原始输入。
【验证方式】解码后的每个字段必须与编码前的输入完全一致，确保协议帧往返无损。`,

	"P02": `【前置场景】服务器响应帧同样需要编解码一致性，且需携带状态码区分成功/失败。
【测试过程】构造4种响应帧（GET成功带值、SET成功空值、未知命令错误、无效key错误），
逐一调用 EncodeResponse 编码，再调用 DecodeResponse 解码，对比命令码、状态码和值数据。
【验证方式】解码后 Command、Status byte、Value 均须与输入一致，确保响应协议可靠。`,

	"P03": `【前置场景】帧的深拷贝用于数据隔离场景（如主从同步），必须保证修改副本不影响原始帧。
【测试过程】创建一个 SET 帧，验证字段值正确；调用 Copy() 获取副本，
验证两个指针不同但内容相等（Equals）；修改原始帧的 Value[0]，验证副本不受影响。
【验证方式】指针不同 → 浅拷贝检测；内容相等 → 数据完整；修改隔离 → 深拷贝确认。`,

	"P04": `【前置场景】协议编码器必须对非法输入做防御性校验，防止恶意或错误数据进入系统。
【测试过程】分别传入 nil key、超过 MaxKeyLength+1 的超长key、超过 MaxValueLength+1 的超长value，
验证 EncodeRequest 每次都返回错误。
【验证方式】三种非法输入均须被拒绝，确保协议层是系统的第一道防线。`,

	"P05": `【前置场景】协议解码器面对畸形数据（网络传输损坏、恶意构造等）必须安全拒绝。
【测试过程】分别传入 nil 数据、仅3字节的过短数据、长度声明与实际数据不符的帧（声明100字节key但只有2字节），
验证 DecodeRequest 每次都返回错误。
【验证方式】三种畸形输入均被安全拒绝，不会触发 panic 或越界访问。`,

	"P06": `【前置场景】帧验证是服务器处理请求前的必要步骤，需检查命令合法性、字段一致性、长度限制。
【测试过程】验证合法帧通过；nil 帧被拒绝；0x99 非法命令码返回 ERROR_UNKNOWN_COMMAND；
KeyLen 声明100但实际仅5字节返回 ERROR_FRAME_MISMATCH；GET 空key返回 ERROR_INVALID_KEY；
超长 key/value 被拒绝。
【验证方式】每种非法帧都有对应的精确错误码，合法帧正常通过。`,

	// ========================================================================
	// LRU 缓存测试（C01-C08）
	// ========================================================================
	"C01": `【前置场景】LRU缓存构造函数是所有缓存操作的基础，必须正确初始化并拒绝无效参数。
【测试过程】创建容量100的缓存，验证初始 Size 为 0 且未满；调用 Clear 验证无报错。
然后分别传入容量 0 和 -1，验证均返回错误。
【验证方式】正常参数创建成功且初始状态正确，非法参数被拒绝。`,

	"C02": `【前置场景】SET/GET/DELETE 是缓存最核心的三个操作，必须保证数据存取和删除的准确性。
【测试过程】SET("key1","value1") 后 Size=1；GET("key1") 返回 "value1" 且 ok=true；
GET("nonexistent") 返回 ok=false；DELETE("key1") 返回 true，Size=0；
DELETE("nonexistent") 返回 false；再次 GET("key1") 返回 ok=false。
【验证方式】每步操作返回值和缓存状态均符合预期。`,

	"C03": `【前置场景】实际场景中经常对同一 key 重复 SET 更新值，缓存应替换旧值且 Size 不增长。
【测试过程】SET("key","old") 后再 SET("key","new")，GET 验证值为 "new"，Size 仍为 1。
【验证方式】值被正确更新，Size 没有因为重复 key 而增长。`,

	"C04": `【前置场景】全量导出用于主从同步、持久化等场景，需要完整输出所有 key-value 对。
【测试过程】写入3条数据后调用 ExportAll()，验证返回 3 个 key 和 3 个 value，
逐一对比内容正确；Clear 后再导出验证为空。
【验证方式】导出的 key-value 与写入一致，Clear 后确实清空。`,

	"C05": `【前置场景】空 key 在缓存系统中没有意义（无法区分不同的空字符串），必须被拒绝。
空 value 则是合法的（可能表示占位符或标记删除）。
【测试过程】SET("","value") 应返回错误；SET("key",[]) 应成功，
GET("key") 返回 ok=true 但 value 长度为 0。
【验证方式】空 key 被拒绝，空 value 被接受且长度为 0。`,

	"C06": `【前置场景】LRU 淘汰是缓存系统的核心策略：容量满时自动移除最久未使用的条目。
【测试过程】创建容量 5 的缓存，依次写入 key-0 到 key-4，验证 Size=5 且 IsFull=true；
写入 key-5 触发淘汰，验证 Size 仍为 5；GET("key-0") 返回 false（最老被淘汰），
GET("key-5") 返回 "val-5"（最新写入存在）。
【验证方式】淘汰后 Size 不超过容量，被淘汰的是最久未访问的 key-0。`,

	"C07": `【前置场景】热点数据应被 LRU 策略保护——频繁访问的数据不会因容量不足被淘汰。
【测试过程】容量 5 写满后，对 key-0 连续 GET 3次（标记为热点），然后写入 key-5 触发淘汰。
验证 key-0 仍然存在（因为被频繁访问），而 key-1 被淘汰（成为新的最久未使用）。
【验证方式】热点数据在淘汰中存活，非热点数据被正确淘汰。`,

	"C08": `【前置场景】删除操作应释放缓存空间，使后续写入不需要触发 LRU 淘汰。
【测试过程】容量 5 写满后，DELETE("key-2") 使 Size=4；再写入 key-5，Size=5。
验证 key-0 仍存在（没有发生淘汰），key-2 确认已删除。
【验证方式】删除释放的空间被新数据利用，未触发 LRU 淘汰。`,

	// ========================================================================
	// 一致性哈希环测试（S01-S07）
	// ========================================================================
	"S01": `【前置场景】一致性哈希环是分布式缓存路由的基础，构造函数需正确初始化并验证参数。
【测试过程】创建 100 个虚拟节点的哈希环，验证初始 NodeCount 为 0；
分别传入虚拟节点数 0 和 -1，验证均返回错误。
【验证方式】正常参数创建成功，空环初始节点数为 0，非法参数被拒绝。`,

	"S02": `【前置场景】添加节点是哈希环的核心操作，每个物理节点会生成 N 个虚拟节点分布到环上。
【测试过程】创建 10 虚拟节点/物理节点的环，AddNode("NodeA") 后 NodeCount=1、VirtualNodeCount=10；
重复添加同一节点（幂等性）NodeCount 仍为 1；AddNode("") 空ID 应报错。
【验证方式】节点计数正确，重复添加幂等，空 ID 被拒绝。`,

	"S03": `【前置场景】节点下线时需从哈希环移除，仅影响被移除节点的数据路由，其他数据不变。
【测试过程】添加 NodeA 和 NodeB（共 20 虚拟节点），RemoveNode("NodeA") 后
NodeCount=1，VirtualNodeCount=10；移除不存在的 "NonExistent" 和空 ID 均报错。
【验证方式】移除后计数正确，异常输入被安全处理。`,

	"S04": `【前置场景】一致性哈希的关键特性：同一个 key 多次路由必须得到相同结果（确定性）。
【测试过程】3 个节点、100 虚拟节点的环，对 100 个不同 key 分别路由两次，
记录第一次结果，第二次逐一对比。
【验证方式】100 个 key 的两次路由结果完全一致，证明哈希函数是确定性的。`,

	"S05": `【前置场景】空环（无节点）是边界场景，路由任意 key 应返回空字符串而非报错。
【测试过程】创建空环，调用 GetNode("any-key")，验证返回 ""。
【验证方式】空环不 panic、不报错，返回空字符串表示无可用节点。`,

	"S06": `【前置场景】一致性哈希的核心目标之一是数据均匀分布，避免热点问题。
【测试过程】3 节点、100 虚拟节点，路由 1000 个 key，统计每个节点分配到的 key 数量。
计算每个节点与均值（333）的偏差百分比，验证偏差不超过 30%。
【验证方式】所有节点偏差在阈值内，虚拟节点实现了较好的均匀分布。`,

	"S07": `【前置场景】一致性哈希的核心目标之二是节点增减时最小化数据迁移。
【测试过程】3 节点路由 500 个 key 记录结果；移除 Node-3 后，原 Node-3 的 key 重路由
到有效节点，其他 key 路由不变；添加 Node-4 后所有 key 仍能路由到有效节点。
【验证方式】移除节点仅影响被移除节点的数据，添加节点不破坏已有路由。`,

	// ========================================================================
	// 缓存节点测试（N01-N06）
	// ========================================================================
	"N01": `【前置场景】缓存节点是 LRU 缓存 + 一致性哈希环的组合体，创建后需 Init+Start 才能使用。
【测试过程】NewCacheNode("test-init-node",100) 创建后验证 NodeID 和初始状态（Stopped）；
Init(ring) + Start() 后状态变为 Running；Stop() 关闭。
空 ID 和零容量创建应报错。
【验证方式】节点状态流转 Stopped→Running→Stopped，非法参数被拒绝。`,

	"N02": `【前置场景】缓存节点对外暴露 SET/GET/DELETE API，需要验证数据通过节点层正确传递到 LRU 缓存。
【测试过程】创建独立测试节点，SET("nk1","nv1")；GET("nk1") 验证值为 "nv1"；
DELETE("nk1") 后再 GET 验证已删除。
【验证方式】节点层的读写删操作结果与直接操作 LRU 缓存一致。`,

	"N03": `【前置场景】节点信息查询用于监控和管理，应返回节点的基本元数据。
【测试过程】创建节点并 SET 一条数据，调用 GetInfo()，验证返回值非 nil。
【验证方式】GetInfo 正常返回，不报错，包含节点元数据。`,

	"N04": `【前置场景】节点状态管理用于主从复制角色切换：普通节点可被设为 Master 或 Slave。
【测试过程】节点 Start 后状态为 Running；SetStatus(Master) 后变为 Master；
SetStatus(Slave) 后变为 Slave；SetMasterID("master-1") 不改变当前 Slave 状态。
【验证方式】状态切换顺序正确，SetMasterID 仅记录 master 信息不影响自身状态。`,

	"N05": `【前置场景】节点数据导出用于主从同步、数据迁移、备份等场景。
【测试过程】创建节点并写入 5 条数据，调用 ExportAll()，验证返回 5 个 key 和 5 个 value。
【验证方式】导出数量与写入数量一致，数据完整。`,

	"N06": `【前置场景】节点有容量上限，超过后应触发 LRU 淘汰而非无限制增长。
【测试过程】创建容量 100 的节点，写入 100 条数据填满；继续写入 overflow，
验证 Size 仍为 100（不超过容量上限）。
【验证方式】容量硬限制生效，溢出写入触发淘汰而非增长。`,

	// ========================================================================
	// TCP 服务测试（T01-T09）
	// ========================================================================
	"T01": `【前置场景】SET/GET 是客户端最常用的操作，需要通过完整的 TCP 协议栈验证端到端正确性。
【测试过程】创建独立临时集群（3节点+TCP服务器），通过 TCP 连接发送 SET("sk1","sv1")，
验证返回 SUCCESS；发送 GET("sk1") 验证值为 "sv1"；GET("nonexistent") 返回空值。
【验证方式】SET 后 GET 数据一致，不存在 key 返回空值，整个 TCP 协议栈工作正常。`,

	"T02": `【前置场景】DELETE 操作需要确保数据真正被清除，GET 后不再返回。
【测试过程】先 SET("dk1","dv1")，发送 DELETE("dk1") 验证返回 SUCCESS；
再 GET("dk1") 验证返回空值，确认数据已被清除。
【验证方式】DELETE 后 GET 返回空值，数据彻底清除。`,

	"T03": `【前置场景】INFO 命令用于查询服务器运行状态，应返回结构化的服务器信息。
【测试过程】发送 INFO 请求（空key），验证返回 SUCCESS 且响应数据长度大于 0。
【验证方式】INFO 成功返回，包含服务器状态数据。`,

	"T04": `【前置场景】完整工作流模拟真实使用场景：写入→读取→再写入→删除→确认删除→读取另一个。
【测试过程】SET("Key1","Value1") → GET 验证 → SET("Key2","Value2") →
DELETE("Key1") → GET("Key1") 空值 → GET("Key2") 值为 "Value2"。
【验证方式】6 步操作全部成功，数据状态在每个步骤后都正确。`,

	"T05": `【前置场景】服务器必须安全处理非法命令（如客户端bug或恶意请求），不影响后续正常操作。
【测试过程】发送命令码 0x99（不存在），验证返回 ERROR_UNKNOWN_COMMAND；
之后再发送正常 SET 请求，验证服务器仍能正常处理。
【验证方式】非法命令被正确拒绝，服务器不崩溃，后续请求正常。`,

	"T06": `【前置场景】空 key 是无效参数，GET/SET/DELETE 均应拒绝并返回精确错误码。
【测试过程】分别发送 GET(空key)、SET(空key,value)、DELETE(空key)，
验证三次均返回 ERROR_INVALID_KEY。
【验证方式】三种命令对空 key 的处理一致，均返回 INVALID_KEY 错误码。`,

	"T07": `【前置场景】客户端可能随时断开（网络故障、进程崩溃），服务器必须优雅处理不影响其他客户端。
【测试过程】客户端1连接后立即 Close()；等待100ms让服务器处理断开事件；
客户端2连接后 SET("after-dc","works")，验证返回 SUCCESS。
【验证方式】客户端1异常断开后，服务器仍能为客户端2正常服务。`,

	"T08": `【前置场景】实际生产中多客户端并发访问是常态，服务器必须正确处理并发请求不串数据。
【测试过程】启动 5 个并发客户端，每个客户端执行 10 次 SET+GET 循环，
每个操作验证 status 和返回值。所有错误收集到 channel 统一报告。
【验证方式】5×10=50 次 SET+50 次 GET 全部成功，无数据串扰。`,

	"T09": `【前置场景】压力测试验证服务器在高并发下的稳定性和数据一致性。
【测试过程】10 个并发客户端，每个执行 20 次 SET+GET（总计 400 次操作），
使用原始协议编码提升吞吐，每个操作都验证 status 和值一致性。
【验证方式】400 次操作全部成功，无并发错误，服务器吞吐量达标。`,

	// ========================================================================
	// 主从复制测试（R01-R07）
	// ========================================================================
	"R01": `【前置场景】主从复制的第一步是配置主从关系，节点状态应自动切换为 Master/Slave。
【测试过程】创建独立临时集群，SetMasterSlave(node0, node1) 后验证 node0 状态为 Master、
node1 状态为 Slave；分别测试空 masterID、空 slaveID、相同节点、不存在的节点ID，均应报错。
【验证方式】正常配置后状态正确，4 种异常输入均被拒绝。`,

	"R02": `【前置场景】主从同步的核心：主节点写入的数据必须能同步到从节点，从节点可读取。
【测试过程】配置 node0(Master)→node1(Slave) 关系；Master SET("rep-key1","rep-val1")；
调用 SyncToSlave 同步到从节点；Slave GET("rep-key1") 验证值为 "rep-val1"；
验证 GetSyncedCount() >= 1。
【验证方式】从节点数据与主节点一致，同步计数正确递增。`,

	"R03": `【前置场景】初始化同步（InitSync）用于从节点首次连接主节点时获取全量数据。
【测试过程】调用 InitSync(有效节点ID) 验证成功；调用 InitSync("") 和 InitSync("NonExistent") 验证报错。
【验证方式】有效ID同步成功，无效输入被安全拒绝。`,

	"R04": `【前置场景】状态查询用于监控主从复制的运行状态，包括 masterID、同步计数、从节点列表等。
【测试过程】初始状态验证 masterID 为空、syncCount 为 0；配置主从后验证 GetMasterID、
GetSlaveIDs、GetNodeCount、GetState 等全部正确返回。
【验证方式】初始状态和配置后状态的每个字段都符合预期。`,

	"R05": `【前置场景】删除操作也需要同步到从节点，确保主从数据一致性。
【测试过程】配置主从后，sync-set("del-key","del-val") 同步数据到 slave；
验证 slave 有数据；调用 SyncDeleteToSlave("del-key")；
验证 slave GET("del-key") 返回 nil（已删除）；空 key 调用应报错。
【验证方式】删除同步后 slave 数据被清除，异常输入被拒绝。`,

	"R06": `【前置场景】全量同步用于从节点数据丢失或新节点加入时，从主节点获取完整数据快照。
【测试过程】Master 写入 30 条数据并逐条同步到 slave，再额外写入 20 条（仅 master 有）。
RequestFullSync 获取 master 全部 50 帧数据，ApplyFullSync 应用到 slave。
逐一验证 slave 的 50 条数据与 master 完全一致。
【验证方式】全量同步后 slave 数据量 = master 数据量，每条数据值一致。`,

	"R07": `【前置场景】实际场景中多个写操作可能并发执行，同步机制必须保证并发安全。
【测试过程】20 个 goroutine 并发执行 master SET + SyncToSlave，每个写入唯一的 key-value。
等待全部完成后验证 slave.Size() == 20，逐一 GET 验证 20 条数据全部正确。
【验证方式】并发同步无数据丢失、无数据错乱，20 条数据完整正确。`,

	// ========================================================================
	// 集成测试（I01-I03）
	// ========================================================================
	"I01": `【前置场景】网络传输中可能出现不完整数据（连接中断、网络延迟等），服务器必须安全处理。
【测试过程】连接后发送 4 字节不完整帧头（协议头需 9 字节），然后立即断开连接；
等待 100ms 让服务器处理异常；新客户端连接后 SET 验证服务器仍正常运行。
【验证方式】截断帧头后服务器不崩溃，新连接能正常工作。`,

	"I02": `【前置场景】某些客户端可能发送恰好只有帧头长度的请求（无额外 body），服务器应正确处理。
【测试过程】手动构造 9 字节 INFO 请求帧（Command=INFO, KeyLen=0, ValueLen=0），
发送后读取 9 字节响应头 + body，验证 status == SUCCESS。
【验证方式】仅帧头的请求被正确处理，响应状态为成功。`,

	"I03": `【前置场景】超大 value 可能导致内存溢出或网络问题，协议层需在编码阶段就拒绝。
【测试过程】构造超过 MaxValueLength+1 的超大 value，EncodeRequest 应返回错误；
构造恰好等于 MaxValueLength 的 value，EncodeRequest 应成功。
【验证方式】超限被拒绝，恰好等于上限被接受，边界值处理正确。`,
}

// getTestDescription 根据测试 ID 查找描述文本
func getTestDescription(id string) string {
	if desc, ok := testDescMap[id]; ok {
		return desc
	}
	return ""
}
